name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy Production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build project
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}

# ─── How to set up secrets ────────────────────────────────────────────────
# In GitHub repo → Settings → Secrets → Actions → New repository secret
#
# VERCEL_TOKEN       → vercel.com → Account Settings → Tokens → Create
# GEMINI_API_KEY     → aistudio.google.com → Get API Key
# NEXT_PUBLIC_SUPABASE_URL      → supabase.com → Project → Settings → API
# NEXT_PUBLIC_SUPABASE_ANON_KEY → same page above
# SUPABASE_SERVICE_ROLE_KEY     → same page above
